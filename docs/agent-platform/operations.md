# Operations

Health:
- Gateway `GET /health`
- Gateway `GET /runtime/health` (auth)
- Hermes `GET /v1/models`

Maintenance: set RuntimeInstance status `maintenance`, queue runs, upgrade pinned image, smoke test, resume.
